# Day18 - pytest 与最小测试

> 阶段二：Python 工程化基本功

## 学习定位

用测试保护函数行为。

今天的目标不是背语法，而是产出：**一个函数配两个测试**。

## 前置知识

- 推荐前置：Day17 函数和日志
- 上一站：Day17
- 下一站：Day19

## 今日知识地图

- assert
- pytest
- 正常案例
- 边界案例

## 核心讲解

今天的关键词是 **用测试保护函数行为**。学习时不要只看语法表面，而要始终追问三件事：输入是什么、处理逻辑是什么、输出要给谁使用。这个习惯会贯穿数据分析、Quant、LLM 应用和 Agent tool 设计。

从工程角度看，今天的能力要服务于 **长期项目的回归保护**。也就是说，代码不只是“能跑”，还要能被未来的自己复查：文件名清楚、变量名能表达含义、失败时有可读错误、结果能保存。

难度承接上，今天依赖 **Day17 函数和日志**；完成后会自然进入 **Day19**。如果今天卡住，优先回看 Day17，不要跳到更复杂的库或项目。

## 知识点结构

### 定义

用测试保护函数行为，是把一个具体问题拆成可运行、可检查、可复用代码的过程。

### 为什么存在

它让学习从临时笔记变成长期资产。公开仓库里的每一天都应该能让读者看到：今天学什么、为什么学、怎么运行、如何检查结果。

### 最小案例

```python
def average(values):
    return sum(values) / len(values) if values else 0

def test_average_normal():
    assert average([1, 2, 3]) == 2

def test_average_empty():
    assert average([]) == 0
```

### 常见错误

- 不要一次性引入多个新库
- 不要提交自己无法解释的代码

### 工程应用

- 数据分析脚本
- Quant 研究模块
- LLM 报告助手
- Agent tool / workflow
- GitHub 开源项目作品集

### 未来扩展

今天只做最小闭环。后续可以逐步加入测试、日志、配置、页面、API 或项目文档，但不要牺牲可解释性。

## 基础详细讲解

### 如果你完全看不懂，先只抓这一句话

今天学的是：**测试就是把“我期望函数返回什么”写成代码。**

```text
给函数一个输入 -> 得到实际输出 -> 和期望输出比较
```

### assert 怎么读

```python
def add(a, b):
    return a + b

assert add(2, 3) == 5
```

这句话意思是：我期望 `add(2, 3)` 的结果等于 5。如果不是 5，测试失败。

### 为什么先测函数

函数最适合测试，因为它通常是：

```text
输入 -> 处理 -> 返回值
```

比如：

```python
def average(values):
    if not values:
        return 0
    return sum(values) / len(values)
```

可以测试：

```python
assert average([1, 2, 3]) == 2
assert average([]) == 0
```

### 测试失败怎么看

测试失败不是坏事。它告诉你：实际结果和期望结果不一样。

| 你要看 | 说明 |
| --- | --- |
| 输入是什么 | 哪个样例触发失败 |
| 期望是什么 | assert 右边写的值 |
| 实际是什么 | 函数真实返回值 |
| 谁错了 | 可能是函数错，也可能是期望写错 |

### 最小测试习惯

每个重要函数至少写三类：

- 正常输入。
- 空值或边界输入。
- 容易写错的输入。

### pytest 与最小测试

测试是把你的预期写成代码。它不是大公司才需要的东西，而是让你确认函数在修改后仍然可靠。最小测试只需要给定输入、调用函数、断言输出。

### 基础知识深讲：测试是可执行预期

测试的核心不是 pytest 语法，而是“把预期写成代码”。当你写 `assert average([1, 2, 3]) == 2`，你是在告诉项目：这个函数在这个输入下必须返回 2。以后代码改动后，测试能自动检查这个预期是否仍然成立。

最适合先测试的是纯函数。纯函数给定输入就返回输出，不依赖终端输入、文件、网络或时间。比如平均值、最大值、收益率计算都适合测试。越纯的函数，越容易测试，也越容易复用。

测试用例要覆盖正常、边界和错误。正常输入证明功能能跑；边界输入证明你考虑了空值、零、单元素；错误输入证明失败方式可控。测试不是为了追求数量，而是为了覆盖关键风险。

失败的测试是一种反馈，不是麻烦。它告诉你实际输出和预期输出不一致。调试时不要急着改代码，先确认是测试预期错了，还是函数逻辑错了。这个判断能力会影响后面所有工程项目。

### 手写顺序

- 先测试纯函数，例如 `add(2, 3) == 5`。
- 每个测试只验证一个行为，不要把很多逻辑塞进一个测试。
- 至少写正常输入、边界输入、错误输入三类案例。

### 今天必须真正弄懂

- **输入**：今天这段代码从哪里拿到数据，是手写值、终端输入、文件，还是函数参数。
- **处理**：每一行代码改变了什么状态，例如变量变了、列表多了一个元素、判断走了某个分支。
- **输出**：结果给谁看，是终端输出、返回值、文件，还是后面函数继续使用。

### 和后面项目的关系

Quant 指标、清洗函数、Agent tool 都需要测试，否则项目越改越不敢动。

### 零基础拆解：pytest 把预期写成代码

#### 这一天到底在练什么

测试不是额外负担，而是把“我认为这个函数应该怎样”写下来。函数改动后，测试能帮你确认旧能力还在。

#### 手写时按这个顺序

- 先写一个纯函数。
- 写一个正常输入测试。
- 再写一个空输入或边界输入测试。

#### 最常见卡点

测试失败不等于坏事，它是在告诉你预期和实际不一致。先看失败的输入、实际输出和期望输出。

#### 自查问题

- 我能写 assert。
- 我能运行 pytest。
- 我能解释一个测试失败的原因。

## 工程化详细讲解

### 如果你完全看不懂，先只抓这一句话

今天这章不是孤立知识点，而是在训练一个真实工程习惯：把「pytest与最小测试」放进一个可以运行、可以检查、可以复用的小流程里。

这一阶段的目标是把基础语法连接到文件、异常、模块和小项目。你要开始习惯“代码会长期运行、别人会复用、以后自己会回来维护”。

### 真实工作流怎么想

你可以按下面这条线理解今天内容：

```text
任务目标 -> 拆成函数 -> 处理异常 -> 保存结果 -> 写 README/复盘
```

这条线的意思是：先不要追求写得高级。先保证你知道数据从哪里来，经过了哪些变量，最后去了哪里。只要能画出这条数据流，你就已经从“背语法”进入“写程序”。

### 执行过程追踪

学习今天内容时，每写 5 到 10 行代码，就问自己四个问题：

1. 现在有哪些变量？
2. 每个变量是什么类型？
3. 下一行代码会改变哪个变量？
4. 如果输出不对，我应该打印哪里？

这四个问题比多背几个函数更重要。以后做数据分析、Agent tool、量化回测时，真正救命的就是这种追踪能力。

### 最容易混的点

- 不要只看最终答案，要看中间变量怎么变化。
- 不要把函数、循环、文件、列表当成分开的知识，它们最后都会组合在一个任务里。
- 不要一开始就追求“优雅写法”。先写清楚，再减少重复。
- 每个例子都要能说清楚 input、process、output。

### 今日最小工程化练习

在今天主代码旁边新建一个 `day18_mini_project.py`，只做三件事：

1. 准备一小份样例数据。
2. 用今天知识点处理它。
3. 打印一个清晰结果，并写一句注释解释这个结果代表什么。

完成后再想一步：如果明天要复用这段代码，哪些地方应该改成函数，哪些参数不应该写死。

## 2 小时学习节奏

- 15 分钟：读定位、前置和知识地图。
- 25 分钟：手打最小案例并运行。
- 35 分钟：完成 7 道简单路线题。
- 35 分钟：完成 5 道基础巩固题。
- 10 分钟：记录 Debug、结果和下一步。

## 简单路线 7 题

1. 读完“学习定位”，用一句话写下今天要解决的问题。
2. 手打最小案例并运行，确认得到 `一个函数配两个测试`。
3. 把示例输入改成 2-3 条虚拟样例，观察输出是否仍然合理。
4. 故意制造一个小错误，记录报错、原因和修复方式。
5. 给输出加一个清晰字段名或标题，避免只看到裸数字。
6. 把今天代码保存到本地练习文件夹，并写下运行命令。
7. 用 3 行话说明它如何服务于 `长期项目的回归保护`。

## 5 道基础巩固题

#### 1. 复现任务

题目：完整复现今日最小案例，不改代码结构，只确认能运行。

讲解：训练“先跑通，再优化”的习惯。

参考解法骨架：

```python
# Day18 - 复现任务
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day18 复现任务")
```

#### 2. 输入替换

题目：替换为新的虚拟输入，观察输出是否仍然合理。

讲解：训练输入边界意识。

参考解法骨架：

```python
# Day18 - 输入替换
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day18 输入替换")
```

#### 3. 边界检查

题目：添加一个空输入、缺字段或非法值检查。

讲解：工业代码必须能解释失败。

参考解法骨架：

```python
# Day18 - 边界检查
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day18 边界检查")
```

#### 4. 结果保存

题目：把输出保存成 Markdown、JSON、CSV、SQLite 记录或截图说明。

讲解：公开项目需要可复查产物。

参考解法骨架：

```python
# Day18 - 结果保存
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day18 结果保存")
```

#### 5. 迁移说明

题目：写一段说明：今天能力如何迁移到 长期项目的回归保护。

讲解：把语法学习变成项目资产。

参考解法骨架：

```python
# Day18 - 迁移说明
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day18 迁移说明")
```

## Debug 记录模板

```text
错误现象：
触发输入：
定位过程：
修复方式：
以后如何避免：
```

## 复习检查

- [ ] 我能解释今天能力的输入、处理和输出。
- [ ] 我能从零手打最小案例。
- [ ] 我完成了 7 道简单路线题和 5 道基础巩固题。
- [ ] 我把结果保存成可复查产物。

## 参考资料

- [pytest 文档](https://docs.pytest.org/)
