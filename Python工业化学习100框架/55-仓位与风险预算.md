# Day55 - 仓位与风险预算

学习定位：学习仓位不是越大越好。今天只做固定风险预算和最大仓位限制。

## 今日前置

- 已完成前面天数的基础能力。
- 今天只增加一个小能力，不把后面项目一次性做完。
- 如果卡住，先回到最小案例，不直接堆新库。

## 今日只允许新增

- position size
- max weight
- risk budget
- clip

## 今日不要做

- 不要今天做杠杆；不要让模型直接决定资金比例。
- 不使用真实身份、真实账号、真实密钥或不可公开数据。
- 不把不能解释的代码提交到项目里。

## 2 小时学习节奏

- 15 分钟：读学习定位和知识地图。
- 25 分钟：手打最小案例并运行。
- 35 分钟：完成 7 道简单路线题。
- 35 分钟：完成 5 道基础巩固题。
- 10 分钟：记录 Debug 和下一步。

## 今日知识地图

- Position Sizing：很多策略失败不是信号错，而是仓位过大。
- Risk Budget：它让策略可控。

## 知识点 1：Position Sizing

### 定义

仓位管理决定每次承担多少风险。

### 为什么存在

很多策略失败不是信号错，而是仓位过大。

### 最小案例

看下面“今日最小可运行任务”，先跑通，再改一处输入。

### 常见错误

- 没有先确认输入字段和输出格式。
- 直接复制复杂代码，但解释不清每一步。
- 跑通后没有保存结果或记录失败情况。

### 工程应用

- 数据分析脚本。
- Quant 研究小模块。
- LLM / Agent 工具边界。
- 个人作品集项目。

### 未来扩展

今天只做最小版本；未来可以加测试、日志、配置、服务接口或页面。

## 知识点 2：Risk Budget

### 定义

风险预算是预先允许的风险上限。

### 为什么存在

它让策略可控。

### 最小案例

看下面“今日最小可运行任务”，先跑通，再改一处输入。

### 常见错误

- 没有先确认输入字段和输出格式。
- 直接复制复杂代码，但解释不清每一步。
- 跑通后没有保存结果或记录失败情况。

### 工程应用

- 数据分析脚本。
- Quant 研究小模块。
- LLM / Agent 工具边界。
- 个人作品集项目。

### 未来扩展

今天只做最小版本；未来可以加测试、日志、配置、服务接口或页面。

## 今日最小可运行任务

```python
def size_position(score: float, max_weight: float = 0.3) -> float:
    weight = score * max_weight
    return max(0.0, min(weight, max_weight))

print(size_position(0.8))
print(size_position(2.0))
```

## Debug 日志

记录这三件事：

- 输入是什么。
- 程序在哪一步失败。
- 修复后输出是什么。

## Quant / LLM / Agent 迁移点

今天的交付物是：写出一个限制最大仓位的函数。它以后可以作为 Quant 数据模块、LLM 报告模块或 Agent tool 的一个稳定零件。

## 简单路线 7 题（不超前）

1. 新建 `day55` 文件夹或脚本。
2. 手打一遍“今日最小可运行任务”，不要复制后直接跳过。
3. 把示例数据改成 2-3 条自己的虚拟数据。
4. 故意制造一个小错误，记录报错信息和修复方式。
5. 给输出结果加一个清晰字段名或标题。
6. 把运行命令写进当天笔记。
7. 用 3 行话总结今天能力如何迁移到 写出一个限制最大仓位的函数。

## 5 道基础巩固题

#### 1. 复现最小案例

题目：完整手打最小案例，并确认输出和预期一致。

讲解：先不改结构，只确认能跑。

参考解法骨架：

```python
# Day55 - 复现最小案例
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day55 复现最小案例")
```

#### 2. 替换输入数据

题目：把示例里的数据改成自己的虚拟数据。

讲解：只改输入，不改处理逻辑。

参考解法骨架：

```python
# Day55 - 替换输入数据
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day55 替换输入数据")
```

#### 3. 增加边界检查

题目：为空数据、缺字段或异常输入加一个检查。

讲解：检查失败时返回清晰错误。

参考解法骨架：

```python
# Day55 - 增加边界检查
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day55 增加边界检查")
```

#### 4. 保存结果

题目：把结果保存为 `.md`、`.json`、`.csv` 或数据库记录。

讲解：工业项目不能只依赖 print。

参考解法骨架：

```python
# Day55 - 保存结果
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day55 保存结果")
```

#### 5. 写复盘说明

题目：写下今天的输入、处理、输出、失败情况。

讲解：保证六个月后还能重新接上。

参考解法骨架：

```python
# Day55 - 写复盘说明
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day55 写复盘说明")
```

## 复习检查

- [ ] 我能解释今天新增的关键词。
- [ ] 我能从零手打最小案例。
- [ ] 我知道今天输入、处理、输出分别是什么。
- [ ] 我完成了 7 道简单路线题和 5 道基础巩固题。
- [ ] 我没有暴露真实身份、账号、密钥或私人数据。

## 参考资料

- [Python 官方文档](https://docs.python.org/3/)
