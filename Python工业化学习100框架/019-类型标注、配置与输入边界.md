# Day19 - 类型标注、配置与输入边界

> 阶段二：Python 工程化基本功

## 学习定位

用类型和配置让代码更可读。

今天的目标不是背定义，而是产出：**一个读取配置并返回结构化结果的函数**。

## 前置知识

- 推荐前置：Day18 测试
- 上一站：Day18
- 下一站：Day20

## 今日知识地图

- type hints
- 默认值
- 输入校验
- ValueError

## 真实任务入口

今天先不从定义开始，而是先看问题：**如何用「类型标注、配置与输入边界」完成 `一个读取配置并返回结构化结果的函数`？**

这个问题在工程里不是孤立练习。它属于 **Python 工程化基本功** 阶段，目标是：把脚本改成可复用、可调试、可测试的小工具。

先按这条顺序学习：

```text
真实任务
  ↓
拆解流程
  ↓
引出问题
  ↓
引出知识点
  ↓
解释设计原因
  ↓
代码实现
  ↓
未来项目应用
```

今天你真正要掌握的不是“会背哪个语法”，而是能回答：它解决什么问题、为什么需要它、怎么落到代码、以后在哪个项目会再次出现。

## 工程问题解决结构

### 1. 是什么（What）

类型标注、配置与输入边界 是用 Python 解决工程任务时的一组小工具。最简定义：它帮助你把「type hints、默认值、输入校验」放进可运行流程里。先把它当成解决问题的方法，不要先背术语。

### 2. 为什么存在（Why）

如果没有 类型标注、配置与输入边界，代码通常会变成一次性脚本：逻辑散、结果难复查、报错难定位、以后项目里也难复用。它存在的意义是把真实任务拆成更清楚的步骤，让你知道哪里输入、哪里处理、哪里输出。

### 3. 真实工程场景（Real Scenario）

- 科研场景：整理实验记录、批量生成学习日志、清洗论文标题或作者字段。
- 数据分析场景：处理一小份 CSV/JSON/文本记录，输出可检查结果。
- Quant 场景：读取几条价格或收益样例，计算简单指标或生成检查日志。
- LLM/Agent 场景：给未来 Agent tool 准备稳定的输入、输出、错误信息和本地文件。

### 4. 怎么做（How）

先看场景流程，再看代码：

```text
真实小任务
  ↓
明确输入
  ↓
拆成 1-3 个步骤
  ↓
写最小代码
  ↓
打印/保存结果
  ↓
记录 Debug
```

```python
# Day19 - 类型标注、配置与输入边界

def solve_task(sample_input):
    """把今天的真实任务压缩成一个最小可运行函数。"""
    result = f"处理完成: {sample_input}"
    return result


def main():
    sample = "虚拟样例输入"
    output = solve_task(sample)
    print(output)


if __name__ == "__main__":
    main()
```

### 5. 工程设计思维（Design）

工程师会多写一层函数、配置、输出文件或 README，是为了让代码不只今天能跑，也能被明天的自己、GitHub 访客和未来项目继续使用。

### 6. 项目中的位置（Where）

这个知识点以后会反复出现在：

- Quant 回测框架的基础函数
- FastAPI / Streamlit 的后台逻辑
- Agent tool 的输入输出处理
- 数据处理脚本
- GitHub 开源作品
- 本地学习实验室

### 7. 掌握检查

学完今天不要只问“代码跑了吗”，要能回答四个问题：

1. 它是什么？
2. 它为什么存在？
3. 它解决什么问题？
4. 它在未来 Quant / LLM / Agent 项目中怎么使用？

### 基础优化补充：问题、场景与代码

**阶段定位**：工程基础。

**解决什么问题（Problem）**：类型标注、配置与输入边界 的核心不是背概念，而是把脚本从“能跑一次”推进到“可配置、可调试、可测试、可复用”。

**真实科研场景**：记录实验脚本的参数、日志、测试样例和失败原因，让复现实验时不靠记忆。

**数据分析场景**：为数据清洗、指标计算和本地数据工具建立稳定入口，减少每次改代码换输入的成本。

**Quant 场景**：为 Quant 脚本准备命令行参数、日志、测试和边界检查，避免回测结果无法复现。

**LLM/Agent 场景**：为 Agent tool 准备清晰的输入参数、运行日志、错误记录和最小测试。

```python
def build_day019_note(input_name, output_name, risk):
    return {
        "day": "Day19",
        "topic": "类型标注、配置与输入边界",
        "input": input_name,
        "output": output_name,
        "risk": risk,
        "next_check": "确认输入、处理步骤、输出和失败点是否清楚",
    }
```

## 基础详细讲解

### 如果你完全看不懂，先只抓这一句话

今天学的是：**类型标注让函数输入输出更清楚，配置让变化的参数集中管理。**

```python
def average(values: list[float]) -> float:
    return sum(values) / len(values)
```

这句话不是让 Python 自动变聪明，而是告诉读者：`values` 应该是一组浮点数，返回值应该是浮点数。

### 类型标注怎么读

| 写法 | 含义 |
| --- | --- |
| `x: int` | x 应该是整数 |
| `name: str` | name 应该是字符串 |
| `values: list[float]` | values 应该是浮点数列表 |
| `-> float` | 函数返回浮点数 |

### 配置是什么

不要把经常变化的参数散落在代码里：

```python
threshold = 0.05
input_path = "data.csv"
```

更好的做法是集中：

```python
config = {
    "threshold": 0.05,
    "input_path": "data.csv",
}
```

以后要改，就知道去哪里找。

### 边界设计怎么想

函数入口先问：

```text
这个输入允许为空吗？
类型对吗？
范围合理吗？
缺字段怎么办？
```

这就是后面 API、Agent tool、Quant 参数实验的基础。

### 类型标注、配置与输入边界

类型标注不是给机器看的装饰，而是给未来读代码的人看的契约。配置把经常变化的参数从代码里拿出来，输入边界则说明什么数据可以进入系统。

### 基础知识深讲：类型标注、配置和边界设计

类型标注是一种可读契约。`def average(values: list[float]) -> float` 告诉读者：这个函数希望接收浮点数列表，并返回浮点数。它不等于运行时自动检查，但能让代码意图更清楚，也方便工具发现一部分问题。

配置是把可变参数从代码逻辑里分离出来。路径、阈值、开关、模型名、样本区间都可能变化；如果它们散落在代码里，修改和复查都会困难。配置文件让“业务参数”和“处理逻辑”分开。

输入边界设计决定系统稳定性。入口处应该检查必要字段、类型、范围和空值；核心函数则尽量处理已经验证过的数据。这样代码结构会更清楚：入口负责防守，核心负责计算。

这一天是从语法转向工程判断的关键。你不只是写能跑的代码，而是在定义别人如何安全地调用你的函数、脚本或工具。后面 Agent tool schema、API request validation、Quant 参数实验都延续这个思路。

### 手写顺序

- 给函数参数和返回值加简单类型，例如 `list[float] -> float`。
- 把路径、阈值、开关放到配置里，不要散落在代码中。
- 在入口处检查输入，在核心函数里保持逻辑干净。

### 今天必须真正弄懂

- **输入**：今天这段代码从哪里拿到数据，是手写值、终端输入、文件，还是函数参数。
- **处理**：每一行代码改变了什么状态，例如变量变了、列表多了一个元素、判断走了某个分支。
- **输出**：结果给谁看，是终端输出、返回值、文件，还是后面函数继续使用。

### 和后面项目的关系

LLM 工具、回测参数、数据任务配置都会越来越多。今天练的是可维护边界。

### 零基础拆解：类型、配置和边界

#### 这一天到底在练什么

类型标注让输入输出更清楚，配置让可变参数集中管理，边界检查让坏输入在入口处被发现。

#### 手写时按这个顺序

- 先给一个函数加参数和返回值类型。
- 把路径或阈值写进 config。
- 在入口处检查必要字段是否存在。

#### 最常见卡点

类型标注不会自动修复逻辑，它主要帮助阅读和工具检查。真正的边界仍然要靠判断代码。

#### 自查问题

- 我能读懂 `list[float] -> float`。
- 我能把阈值从代码移到配置。
- 我能写一个输入边界检查。

## 工程工作流补充

### 如果你完全看不懂，先只抓这一句话

今天这章不是孤立知识点，而是在训练一个真实工程习惯：把「类型标注、配置与输入边界」放进一个可以运行、可以检查、可以复用的小流程里。

这一阶段的目标是把基础语法连接到文件、异常、模块和小项目。你要开始习惯“代码会长期运行、别人会复用、以后自己会回来维护”。

### 真实工作流怎么想

你可以按下面这条线理解今天内容：

```text
任务目标 -> 拆成函数 -> 处理异常 -> 保存结果 -> 写 README/复盘
```

这条线的意思是：先不要追求写得高级。先保证你知道数据从哪里来，经过了哪些变量，最后去了哪里。只要能画出这条数据流，你就已经从“背写法”进入“解决问题”。

### 执行过程追踪

学习今天内容时，每写 5 到 10 行代码，就问自己四个问题：

1. 现在有哪些变量？
2. 每个变量是什么类型？
3. 下一行代码会改变哪个变量？
4. 如果输出不对，我应该打印哪里？

这四个问题比多背几个函数更重要。以后做数据分析、Agent tool、量化回测时，真正救命的就是这种追踪能力。

### 最容易混的点

- 不要只看最终答案，要看中间变量怎么变化。
- 不要把函数、循环、文件、列表当成分开的知识，它们最后都会组合在一个任务里。
- 不要一开始就追求“优雅写法”。先写清楚，再减少重复。
- 每个例子都要能说清楚 input、process、output。

### 今日最小工程化练习

在今天主代码旁边新建一个 `day19_mini_project.py`，只做三件事：

1. 准备一小份样例数据。
2. 用今天知识点处理它。
3. 打印一个清晰结果，并写一句注释解释这个结果代表什么。

完成后再想一步：如果明天要复用这段代码，哪些地方应该改成函数，哪些参数不应该写死。

### 知识广度补充：类型标注不是运行时保险

类型标注帮助人和工具理解代码，但 Python 默认不会因为标注自动拦截错误。

你要区分：

- 类型标注：告诉读者预期类型。
- 输入校验：运行时真的检查数据。
- 配置默认值：缺配置时能否继续。
- 环境变量：适合放本地路径、密钥名、运行模式。

```python
def normalize_ticker(ticker: str) -> str:
    if not isinstance(ticker, str):
        raise TypeError("ticker must be str")
    return ticker.strip().upper()
```

长期项目里，类型标注和输入校验经常一起出现：一个帮助阅读，一个保护运行。

### 工业化知识百科补充：从会用到会迁移

这一节不是把前面没学完的语法硬塞回来，而是把 `类型标注、配置与输入边界` 放进后续真实项目里补厚。你现在已经进入 Day10 之后，学习重点要从“我知道这个语法”升级为“我知道它在工程里解决什么问题、会踩什么坑、怎么迁移到项目”。

#### 1. 阶段定位

当前阶段：**工程化基本功**。

阶段目标：把零散语法变成能反复运行、能检查、能复盘的小工具。

本阶段反复出现的能力：

- 输入校验
- 循环控制
- 函数边界
- 文件持久化
- 日志与测试
- 命令行参数

#### 2. 这个主题必须补齐的知识面

- 类型提示
- 运行时校验
- 配置默认值
- 环境变量
- 路径配置
- 输入边界

学习时不要把这些点当成孤立词条。更好的读法是：每个概念都对应一种工程压力，例如输入变多、数据变脏、文件变大、结果要复现、输出要交给别人、模型回答需要校验。

#### 3. 真实项目里怎么用

- **数据分析**：先明确字段、类型、缺失值、样本范围，再写处理逻辑。不要直接对未知数据写复杂代码。
- **Quant**：任何计算都要关心时间顺序、样本区间、交易成本、未来函数和结果复现。
- **LLM/Agent**：所有输入输出都要有结构，模型结果要校验，高风险动作要有人类审查点。
- **开源作品集**：代码之外还要有 README、样例输入、运行命令、测试和复盘说明。

#### 4. 常见坑和边界

- 类型标注不等于运行时检查。
- 公开项目不要把密钥写进配置文件。
- 配置应该让脚本换输入时不用改代码。
- 不要把所有逻辑堆进一个文件或一个函数。先拆出最小核心函数，再决定是否需要 CLI、API、看板或测试。
- 不要用“能跑一次”代表掌握。真正掌握是换一份输入、换一个场景、换一个项目后仍然能迁移。

#### 5. 最小迁移代码骨架

```python
def day019_engineering_note(input_data):
    """记录今天知识点在工程项目里的使用边界。"""
    return {
        "day": "Day19",
        "topic": "类型标注、配置与输入边界",
        "input_shape": "明确输入来源、字段、类型和边界",
        "core_step": "把今天主题压缩成一个可测试的小函数",
        "output_shape": "明确输出给终端、文件、API、报告还是下游函数",
        "failure_modes": ["空输入", "类型不对", "缺字段", "路径或网络失败"],
        "reuse_target": "数据分析 / Quant / LLM-Agent / 作品集项目",
    }
```

#### 6. 百科式自查问题

1. 这个主题的输入是什么？输入可能为空、缺字段、类型错误吗？
2. 核心处理步骤能不能写成一个小函数？
3. 输出是给人看，还是给下游程序继续用？
4. 失败时怎么记录？是 `print`、`logging`、错误报告，还是测试失败？
5. 如果迁移到 Quant / LLM / Agent 项目，最大的风险是什么？
6. 这个能力是否需要 README、样例数据或测试来长期保存？

#### 7. 建议扩展练习

- 写一个最小正常样例。
- 写一个边界样例，例如空输入、缺字段、类型错误、路径不存在。
- 写一段 Debug 记录，说明错误现象、定位过程和修复方式。
- 把今天的代码改造成一个函数，并说明输入和输出。
- 思考它未来会放进哪个项目模块：`data_io`、`transform`、`analysis`、`api`、`app`、`agent_tool`、`backtest` 或 `report`。

## 多元化操作题（从 Day11 开始）

1. 把 5 条学习记录保存为 CSV。
2. 读取 JSON 配置并校验字段。
3. 写一个带 --input/--output 的 CLI。
4. 跳过空记录并继续处理。
5. 遇到第一个严重错误就 break。
6. 把结果追加写入日志文件。
7. 把核心逻辑拆成函数并测试。
8. 把输出改成 Agent tool 的 ok/data/error 格式。

## Hot100 / LeetCode 题组（多题 + 多解法）

1. [Valid Parentheses - 有效的括号](https://leetcode.com/problems/valid-parentheses/)
   - 题型：栈 / 结构匹配
   - 多解法：栈；替换消除法；递归匹配但不推荐
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：遇到左括号就入栈，遇到右括号就检查栈顶是否匹配。栈保存的是“还没被关闭的期待”。边界是右括号先出现、类型不匹配、最后栈里还有残留。这个题是解析器和状态机的入门。
2. [Maximum Depth of Binary Tree - 二叉树最大深度](https://leetcode.com/problems/maximum-depth-of-binary-tree/)
   - 题型：树 / DFS
   - 多解法：递归 DFS；队列 BFS；栈模拟 DFS
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：树的深度等于左右子树最大深度加一。递归法把大问题拆成同结构小问题；BFS 按层遍历能显式数层数；DFS 栈能避免递归深度限制。重点是空节点返回 0。
3. [Number of Islands - 岛屿数量](https://leetcode.com/problems/number-of-islands/)
   - 题型：图 / DFS / BFS
   - 多解法：DFS 沉岛；BFS 队列；并查集
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：把二维网格当成图，陆地相邻就属于同一个连通块。DFS/BFS 都是在发现一块陆地后把整座岛标记为已访问，避免重复计数。关键是边界检查和访问标记，工程里可迁移到区域聚类、连通组件。
4. [Climbing Stairs - 爬楼梯](https://leetcode.com/problems/climbing-stairs/)
   - 题型：动态规划
   - 多解法：递归；DP 数组；两个变量滚动更新
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：到第 n 阶只能从 n-1 或 n-2 来，所以状态转移是 f(n)=f(n-1)+f(n-2)。递归能看懂关系但会重复计算；DP 数组保存中间结果；滚动变量最省空间。重点是 base case。
5. [Coin Change - 零钱兑换](https://leetcode.com/problems/coin-change/)
   - 题型：动态规划
   - 多解法：暴力搜索；记忆化递归；自底向上 DP
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：目标是用最少硬币凑出金额，本质是“在多个选择里取最优”。暴力搜索会重复探索；记忆化缓存子问题；自底向上 DP 从小金额推到大金额。关键是 unreachable 状态要用无穷大表示。
6. [Two Sum - 两数之和](https://leetcode.com/problems/two-sum/)
   - 题型：数组 / 哈希表
   - 多解法：暴力双循环；哈希表一次遍历；排序+双指针但要保留下标
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：先把问题翻译成“当前数还差谁”。暴力法是在所有组合里找答案；哈希表法是一边走一边记住见过的数，把查找从 O(n) 降到接近 O(1)。重点是先查 need，再存 num，避免同一个元素用两次。

### 一题多解法拆解：Valid Parentheses

- 解法 1：栈。
- 解法 2：替换消除法。
- 解法 3：递归匹配但不推荐。

推荐先写最容易懂的版本。

再写复杂度更好的版本。

最后写一句：这个解法能迁移到今天哪个操作题。

### 带注释参考代码：Valid Parentheses

```python
def is_valid_parentheses(s):
    pairs = {")": "(", "]": "[", "}": "{"}
    stack = []
    for ch in s:
        if ch in pairs.values():
            stack.append(ch)
        elif ch in pairs:
            if not stack or stack.pop() != pairs[ch]:
                return False
    return len(stack) == 0
```

### 做题复盘模板

```text
题目：
LeetCode 官方链接：
提交日期：
提交结果：未做 / 已尝试 / Accepted
输入：
输出：
暴力解法：
优化解法：
关键状态变量：
最容易错的边界：
一题多解法对比：
能迁移到今天哪个操作题：
可写进简历/面试的表达：
100 天能力自检：如果我是统计/金融/数据背景、已经学完前 10 天的人，今天这节是否让我离“较强 Python 能力”更近一步？证据是什么？
```

## 2 小时学习节奏

- 15 分钟：读定位、前置和知识地图。
- 25 分钟：手打最小案例并运行。
- 35 分钟：完成 7 道简单路线题。
- 35 分钟：完成 5 道基础巩固题。
- 10 分钟：记录 Debug、结果和下一步。

## 简单路线 7 题

1. 读完“学习定位”，用一句话写下今天要解决的问题。
2. 手打最小案例并运行，确认得到 `一个读取配置并返回结构化结果的函数`。
3. 把示例输入改成 2-3 条虚拟样例，观察输出是否仍然合理。
4. 故意制造一个小错误，记录报错、原因和修复方式。
5. 给输出加一个清晰字段名或标题，避免只看到裸数字。
6. 把今天代码保存到本地练习文件夹，并写下运行命令。
7. 用 3 行话说明它如何服务于 `API、Agent tool 和数据管道`。

## 5 道基础巩固题

#### 1. 复现任务

题目：完整复现今日最小案例，不改代码结构，只确认能运行。

讲解：训练“先跑通，再优化”的习惯。

参考解法骨架：

```python
# Day19 - 复现任务
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day19 复现任务")
```

#### 2. 输入替换

题目：替换为新的虚拟输入，观察输出是否仍然合理。

讲解：训练输入边界意识。

参考解法骨架：

```python
# Day19 - 输入替换
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day19 输入替换")
```

#### 3. 边界检查

题目：添加一个空输入、缺字段或非法值检查。

讲解：工业代码必须能解释失败。

参考解法骨架：

```python
# Day19 - 边界检查
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day19 边界检查")
```

#### 4. 结果保存

题目：把输出保存成 Markdown、JSON、CSV、SQLite 记录或截图说明。

讲解：公开项目需要可复查产物。

参考解法骨架：

```python
# Day19 - 结果保存
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day19 结果保存")
```

#### 5. 迁移说明

题目：写一段说明：今天能力如何迁移到 API、Agent tool 和数据管道。

讲解：把语法学习变成项目资产。

参考解法骨架：

```python
# Day19 - 迁移说明
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day19 迁移说明")
```

## Debug 记录模板

```text
错误现象：
触发输入：
定位过程：
修复方式：
以后如何避免：
```

## 复习检查

- [ ] 我能解释今天能力的输入、处理和输出。
- [ ] 我能从零手打最小案例。
- [ ] 我完成了 7 道简单路线题和 5 道基础巩固题。
- [ ] 我把结果保存成可复查产物。

## 参考资料

- [Python 官方文档](https://docs.python.org/3/)
