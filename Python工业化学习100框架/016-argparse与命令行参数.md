# Day16 - argparse 与命令行参数

> 阶段二：Python 工程化基本功

## 学习定位

让脚本支持从终端传参。

今天的目标不是背定义，而是产出：**一个带参数的 CLI 脚本**。

## 前置知识

- 推荐前置：Day15 main 入口
- 上一站：Day15
- 下一站：Day17

## 今日知识地图

- argparse
- 默认值
- 类型转换
- 运行说明

## 真实任务入口

今天先不从定义开始，而是先看问题：**如何用「argparse 与命令行参数」完成 `一个带参数的 CLI 脚本`？**

这个问题在工程里不是孤立练习。它属于 **Python 工程化基本功** 阶段，目标是：把脚本改成可复用、可调试、可测试的小工具。

先按这条顺序学习：

```text
真实任务
  ↓
拆解流程
  ↓
引出问题
  ↓
引出知识点
  ↓
解释设计原因
  ↓
代码实现
  ↓
未来项目应用
```

今天你真正要掌握的不是“会背哪个语法”，而是能回答：它解决什么问题、为什么需要它、怎么落到代码、以后在哪个项目会再次出现。

## 工程问题解决结构

### 1. 是什么（What）

argparse 与命令行参数 是用 Python 解决工程任务时的一组小工具。最简定义：它帮助你把「argparse、默认值、类型转换」放进可运行流程里。先把它当成解决问题的方法，不要先背术语。

### 2. 为什么存在（Why）

如果没有 argparse 与命令行参数，代码通常会变成一次性脚本：逻辑散、结果难复查、报错难定位、以后项目里也难复用。它存在的意义是把真实任务拆成更清楚的步骤，让你知道哪里输入、哪里处理、哪里输出。

### 3. 真实工程场景（Real Scenario）

- 科研场景：整理实验记录、批量生成学习日志、清洗论文标题或作者字段。
- 数据分析场景：处理一小份 CSV/JSON/文本记录，输出可检查结果。
- Quant 场景：读取几条价格或收益样例，计算简单指标或生成检查日志。
- LLM/Agent 场景：给未来 Agent tool 准备稳定的输入、输出、错误信息和本地文件。

### 4. 怎么做（How）

先看场景流程，再看代码：

```text
真实小任务
  ↓
明确输入
  ↓
拆成 1-3 个步骤
  ↓
写最小代码
  ↓
打印/保存结果
  ↓
记录 Debug
```

```python
# Day16 - argparse 与命令行参数

def solve_task(sample_input):
    """把今天的真实任务压缩成一个最小可运行函数。"""
    result = f"处理完成: {sample_input}"
    return result


def main():
    sample = "虚拟样例输入"
    output = solve_task(sample)
    print(output)


if __name__ == "__main__":
    main()
```

### 5. 工程设计思维（Design）

工程师会多写一层函数、配置、输出文件或 README，是为了让代码不只今天能跑，也能被明天的自己、GitHub 访客和未来项目继续使用。

### 6. 项目中的位置（Where）

这个知识点以后会反复出现在：

- Quant 回测框架的基础函数
- FastAPI / Streamlit 的后台逻辑
- Agent tool 的输入输出处理
- 数据处理脚本
- GitHub 开源作品
- 本地学习实验室

### 7. 掌握检查

学完今天不要只问“代码跑了吗”，要能回答四个问题：

1. 它是什么？
2. 它为什么存在？
3. 它解决什么问题？
4. 它在未来 Quant / LLM / Agent 项目中怎么使用？

### 基础优化补充：问题、场景与代码

**阶段定位**：工程基础。

**解决什么问题（Problem）**：argparse 与命令行参数 的核心不是背概念，而是把脚本从“能跑一次”推进到“可配置、可调试、可测试、可复用”。

**真实科研场景**：记录实验脚本的参数、日志、测试样例和失败原因，让复现实验时不靠记忆。

**数据分析场景**：为数据清洗、指标计算和本地数据工具建立稳定入口，减少每次改代码换输入的成本。

**Quant 场景**：为 Quant 脚本准备命令行参数、日志、测试和边界检查，避免回测结果无法复现。

**LLM/Agent 场景**：为 Agent tool 准备清晰的输入参数、运行日志、错误记录和最小测试。

```python
def build_day016_note(input_name, output_name, risk):
    return {
        "day": "Day16",
        "topic": "argparse 与命令行参数",
        "input": input_name,
        "output": output_name,
        "risk": risk,
        "next_check": "确认输入、处理步骤、输出和失败点是否清楚",
    }
```

## 基础详细讲解

### 如果你完全看不懂，先只抓这一句话

今天学的是：**命令行参数让你不用改代码，也能给脚本换输入。**

没有参数：

```python
input_path = "data.csv"
```

每次换文件都要改代码。

有参数：

```bash
python3 app.py --input data.csv
```

运行时传入路径。

### argparse 怎么读

```python
import argparse

parser = argparse.ArgumentParser()
parser.add_argument("--input")
args = parser.parse_args()
print(args.input)
```

执行顺序：

| 代码 | 发生了什么 |
| --- | --- |
| `ArgumentParser()` | 创建参数解析器 |
| `add_argument("--input")` | 声明脚本支持一个 input 参数 |
| `parse_args()` | 读取命令行传进来的值 |
| `args.input` | 取出 input 的值 |

### 参数是什么

```bash
python3 app.py --input data.csv --limit 10
```

可以理解成：

```text
input = "data.csv"
limit = "10"
```

注意命令行传进来的值通常先是字符串，要计算时再转换。

### Debug 对照表

| 现象 | 常见原因 | 怎么改 |
| --- | --- | --- |
| 参数取不到 | 没有 add_argument | 先声明参数 |
| 参数名不对 | `--input` 和 `args.xxx` 不一致 | 检查名称 |
| 数字不能计算 | 参数是字符串 | 设置 `type=int` 或手动转换 |
| 不知道怎么用 | help 没写 | 给参数加 `help=` |

### argparse 与命令行参数

命令行参数让脚本从“每次改代码”变成“运行时传入不同输入”。这就是工具化的开始。你写的脚本要能被自己、别人或自动化任务重复调用。

### 基础知识深讲：CLI 参数和工具化思维

命令行参数让脚本从“一次性代码”变成“可重复工具”。如果输入路径、输出路径、阈值都写死在代码里，每次换数据都要改源码；有了 argparse，运行时传参数就能改变行为。

参数其实是另一种输入边界。`--input data.csv` 告诉程序数据从哪里来，`--output report.md` 告诉程序结果放哪里。参数名、默认值、help 文案共同构成工具的使用说明。好的 CLI 不需要作者在旁边解释。

必填参数和默认参数要有取舍。必填参数适合没有它就无法运行的输入，比如 input 文件；默认参数适合有合理常用值的配置，比如输出目录或日志级别。乱用必填会让工具难用，乱用默认会让行为不透明。

CLI 是后面工程化的关键入口。数据清洗脚本、回测脚本、评估脚本、批处理任务，都可以通过命令行参数连接起来。今天学 argparse，其实是在学项目如何被自动化调用。

### 手写顺序

- 先支持一个参数，例如 `--input`，再逐步增加。
- 给参数写清楚 help 文案，未来复查时会省很多时间。
- 给默认值，让脚本在最小情况下也能运行。

### 今天必须真正弄懂

- **输入**：今天这段代码从哪里拿到数据，是手写值、终端输入、文件，还是函数参数。
- **处理**：每一行代码改变了什么状态，例如变量变了、列表多了一个元素、判断走了某个分支。
- **输出**：结果给谁看，是终端输出、返回值、文件，还是后面函数继续使用。

### 和后面项目的关系

数据管道、批处理、回测脚本、评估脚本都会通过 CLI 参数控制输入输出。

### 零基础拆解：命令行参数让脚本可复用

#### 这一天到底在练什么

命令行参数把可变信息从代码里拿出来。以后你不需要每次改源代码，只要运行时传入不同参数。

#### 手写时按这个顺序

- 先加一个 `--input` 参数。
- 给参数设置 help 和默认值。
- 运行两次不同命令，比较输出变化。

#### 最常见卡点

参数名拼错、忘记传必填参数、路径带空格，是常见问题。先运行 `--help` 看脚本说明。

#### 自查问题

- 我能用 `--help` 查看参数。
- 我能传入不同 input 路径。
- 我能解释默认值的作用。

## 工程工作流补充

### 如果你完全看不懂，先只抓这一句话

今天这章不是孤立知识点，而是在训练一个真实工程习惯：把「argparse与命令行参数」放进一个可以运行、可以检查、可以复用的小流程里。

这一阶段的目标是把 list、dict、字符串、函数组合起来，开始像工程任务一样处理数据。重点不是一次写很多代码，而是每一步都能解释清楚。

### 真实工作流怎么想

你可以按下面这条线理解今天内容：

```text
原始数据 -> 清洗字段 -> 遍历统计 -> 封装函数 -> 用样例验证
```

这条线的意思是：先不要追求写得高级。先保证你知道数据从哪里来，经过了哪些变量，最后去了哪里。只要能画出这条数据流，你就已经从“背写法”进入“解决问题”。

### 执行过程追踪

学习今天内容时，每写 5 到 10 行代码，就问自己四个问题：

1. 现在有哪些变量？
2. 每个变量是什么类型？
3. 下一行代码会改变哪个变量？
4. 如果输出不对，我应该打印哪里？

这四个问题比多背几个函数更重要。以后做数据分析、Agent tool、量化回测时，真正救命的就是这种追踪能力。

### 最容易混的点

- 不要只看最终答案，要看中间变量怎么变化。
- 不要把函数、循环、文件、列表当成分开的知识，它们最后都会组合在一个任务里。
- 不要一开始就追求“优雅写法”。先写清楚，再减少重复。
- 每个例子都要能说清楚 input、process、output。

### 今日最小工程化练习

在今天主代码旁边新建一个 `day16_mini_project.py`，只做三件事：

1. 准备一小份样例数据。
2. 用今天知识点处理它。
3. 打印一个清晰结果，并写一句注释解释这个结果代表什么。

完成后再想一步：如果明天要复用这段代码，哪些地方应该改成函数，哪些参数不应该写死。

### 知识广度补充：CLI 参数要有帮助信息和默认值

`argparse` 里常见的基础能力：

- `required=True`：必须传。
- `default=`：默认值。
- `type=`：转换类型。
- `choices=`：限制可选值。
- `help=`：写清楚参数用途。

```python
import argparse

parser = argparse.ArgumentParser()
parser.add_argument("--mode", choices=["csv", "json"], default="csv")
parser.add_argument("--limit", type=int, default=10)
args = parser.parse_args()

print(args.mode, args.limit)
```

好的 CLI 不只是能跑，还要让别人通过 `--help` 知道怎么跑。

### 工业化知识百科补充：从会用到会迁移

这一节不是把前面没学完的语法硬塞回来，而是把 `argparse 与命令行参数` 放进后续真实项目里补厚。你现在已经进入 Day10 之后，学习重点要从“我知道这个语法”升级为“我知道它在工程里解决什么问题、会踩什么坑、怎么迁移到项目”。

#### 1. 阶段定位

当前阶段：**工程化基本功**。

阶段目标：把零散语法变成能反复运行、能检查、能复盘的小工具。

本阶段反复出现的能力：

- 输入校验
- 循环控制
- 函数边界
- 文件持久化
- 日志与测试
- 命令行参数

#### 2. 这个主题必须补齐的知识面

- required
- default
- type
- choices
- help
- 子命令
- 退出码

学习时不要把这些点当成孤立词条。更好的读法是：每个概念都对应一种工程压力，例如输入变多、数据变脏、文件变大、结果要复现、输出要交给别人、模型回答需要校验。

#### 3. 真实项目里怎么用

- **数据分析**：先明确字段、类型、缺失值、样本范围，再写处理逻辑。不要直接对未知数据写复杂代码。
- **Quant**：任何计算都要关心时间顺序、样本区间、交易成本、未来函数和结果复现。
- **LLM/Agent**：所有输入输出都要有结构，模型结果要校验，高风险动作要有人类审查点。
- **开源作品集**：代码之外还要有 README、样例输入、运行命令、测试和复盘说明。

#### 4. 常见坑和边界

- 参数必须有帮助信息。
- 路径、模式、数量这类输入适合做 CLI 参数。
- 复杂 CLI 先从一个命令开始，不要急着做多级子命令。
- 不要把所有逻辑堆进一个文件或一个函数。先拆出最小核心函数，再决定是否需要 CLI、API、看板或测试。
- 不要用“能跑一次”代表掌握。真正掌握是换一份输入、换一个场景、换一个项目后仍然能迁移。

#### 5. 最小迁移代码骨架

```python
def day016_engineering_note(input_data):
    """记录今天知识点在工程项目里的使用边界。"""
    return {
        "day": "Day16",
        "topic": "argparse 与命令行参数",
        "input_shape": "明确输入来源、字段、类型和边界",
        "core_step": "把今天主题压缩成一个可测试的小函数",
        "output_shape": "明确输出给终端、文件、API、报告还是下游函数",
        "failure_modes": ["空输入", "类型不对", "缺字段", "路径或网络失败"],
        "reuse_target": "数据分析 / Quant / LLM-Agent / 作品集项目",
    }
```

#### 6. 百科式自查问题

1. 这个主题的输入是什么？输入可能为空、缺字段、类型错误吗？
2. 核心处理步骤能不能写成一个小函数？
3. 输出是给人看，还是给下游程序继续用？
4. 失败时怎么记录？是 `print`、`logging`、错误报告，还是测试失败？
5. 如果迁移到 Quant / LLM / Agent 项目，最大的风险是什么？
6. 这个能力是否需要 README、样例数据或测试来长期保存？

#### 7. 建议扩展练习

- 写一个最小正常样例。
- 写一个边界样例，例如空输入、缺字段、类型错误、路径不存在。
- 写一段 Debug 记录，说明错误现象、定位过程和修复方式。
- 把今天的代码改造成一个函数，并说明输入和输出。
- 思考它未来会放进哪个项目模块：`data_io`、`transform`、`analysis`、`api`、`app`、`agent_tool`、`backtest` 或 `report`。

## 多元化操作题（从 Day11 开始）

1. 把 5 条学习记录保存为 CSV。
2. 读取 JSON 配置并校验字段。
3. 写一个带 --input/--output 的 CLI。
4. 跳过空记录并继续处理。
5. 遇到第一个严重错误就 break。
6. 把结果追加写入日志文件。
7. 把核心逻辑拆成函数并测试。
8. 把输出改成 Agent tool 的 ok/data/error 格式。

## Hot100 / LeetCode 题组（多题 + 多解法）

1. [Subarray Sum Equals K - 和为 K 的子数组](https://leetcode.com/problems/subarray-sum-equals-k/)
   - 题型：前缀和 / 哈希表
   - 多解法：暴力枚举区间；前缀和数组；前缀和+计数字典
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：暴力枚举所有区间会超时；前缀和把区间和转成 prefix[j] - prefix[i]。哈希表记录前缀和出现次数，看到 prefix-k 就能知道有多少旧位置可组成答案。重点是先放入 0:1。
2. [Lowest Common Ancestor of a Binary Tree - 二叉树的最近公共祖先](https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/)
   - 题型：树 / 递归
   - 多解法：路径记录；递归返回命中；父指针集合
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：如果左右子树分别找到目标，当前节点就是祖先。训练递归返回值承载信息，而不是只遍历。
3. [Course Schedule - 课程表](https://leetcode.com/problems/course-schedule/)
   - 题型：图 / 拓扑排序
   - 多解法：DFS 检环；BFS 入度；邻接表建图
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：课程依赖可以看成有向图，能完成课程等价于没有环。训练依赖关系建模，和任务编排、包依赖很像。
4. [Clone Graph - 克隆图](https://leetcode.com/problems/clone-graph/)
   - 题型：图 / DFS / 哈希表
   - 多解法：DFS 克隆；BFS 克隆；递归+映射表
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：图里有环，不能简单递归复制。需要用映射表记录原节点到新节点，避免重复创建和死循环。
5. [Pacific Atlantic Water Flow - 太平洋大西洋水流问题](https://leetcode.com/problems/pacific-atlantic-water-flow/)
   - 题型：矩阵 / 反向 DFS
   - 多解法：从每格出发；从海岸反向搜索；BFS 多源搜索
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：从每个点出发会重复太多。反过来从海岸向高处搜索，两个可达集合求交集。训练反向思考和多源搜索。
6. [Rotting Oranges - 腐烂的橘子](https://leetcode.com/problems/rotting-oranges/)
   - 题型：BFS / 多源扩散
   - 多解法：逐轮扫描；多源 BFS；时间戳 BFS
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：所有腐烂橘子同时扩散，所以用多源 BFS 按层计时。训练传播过程、队列和时间步模拟。

### 一题多解法拆解：Subarray Sum Equals K

- 解法 1：暴力枚举区间。
- 解法 2：前缀和数组。
- 解法 3：前缀和+计数字典。

推荐先写最容易懂的版本。

再写复杂度更好的版本。

最后写一句：这个解法能迁移到今天哪个操作题。

### 带注释参考代码：Subarray Sum Equals K

```python
def subarray_sum(nums, k):
    prefix = 0
    counts = {0: 1}
    total = 0
    for num in nums:
        prefix += num
        total += counts.get(prefix - k, 0)
        counts[prefix] = counts.get(prefix, 0) + 1
    return total
```

### 做题复盘模板

```text
题目：
LeetCode 官方链接：
提交日期：
提交结果：未做 / 已尝试 / Accepted
输入：
输出：
暴力解法：
优化解法：
关键状态变量：
最容易错的边界：
一题多解法对比：
能迁移到今天哪个操作题：
可写进简历/面试的表达：
```

## 2 小时学习节奏

- 15 分钟：读定位、前置和知识地图。
- 25 分钟：手打最小案例并运行。
- 35 分钟：完成 7 道简单路线题。
- 35 分钟：完成 5 道基础巩固题。
- 10 分钟：记录 Debug、结果和下一步。

## 简单路线 7 题

1. 读完“学习定位”，用一句话写下今天要解决的问题。
2. 手打最小案例并运行，确认得到 `一个带参数的 CLI 脚本`。
3. 把示例输入改成 2-3 条虚拟样例，观察输出是否仍然合理。
4. 故意制造一个小错误，记录报错、原因和修复方式。
5. 给输出加一个清晰字段名或标题，避免只看到裸数字。
6. 把今天代码保存到本地练习文件夹，并写下运行命令。
7. 用 3 行话说明它如何服务于 `可复现运行、自动化和 GitHub 示例`。

## 5 道基础巩固题

#### 1. 复现任务

题目：完整复现今日最小案例，不改代码结构，只确认能运行。

讲解：训练“先跑通，再优化”的习惯。

参考解法骨架：

```python
# Day16 - 复现任务
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day16 复现任务")
```

#### 2. 输入替换

题目：替换为新的虚拟输入，观察输出是否仍然合理。

讲解：训练输入边界意识。

参考解法骨架：

```python
# Day16 - 输入替换
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day16 输入替换")
```

#### 3. 边界检查

题目：添加一个空输入、缺字段或非法值检查。

讲解：工业代码必须能解释失败。

参考解法骨架：

```python
# Day16 - 边界检查
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day16 边界检查")
```

#### 4. 结果保存

题目：把输出保存成 Markdown、JSON、CSV、SQLite 记录或截图说明。

讲解：公开项目需要可复查产物。

参考解法骨架：

```python
# Day16 - 结果保存
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day16 结果保存")
```

#### 5. 迁移说明

题目：写一段说明：今天能力如何迁移到 可复现运行、自动化和 GitHub 示例。

讲解：把语法学习变成项目资产。

参考解法骨架：

```python
# Day16 - 迁移说明
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day16 迁移说明")
```

## Debug 记录模板

```text
错误现象：
触发输入：
定位过程：
修复方式：
以后如何避免：
```

## 复习检查

- [ ] 我能解释今天能力的输入、处理和输出。
- [ ] 我能从零手打最小案例。
- [ ] 我完成了 7 道简单路线题和 5 道基础巩固题。
- [ ] 我把结果保存成可复查产物。

## 参考资料

- [Python 官方文档](https://docs.python.org/3/)
