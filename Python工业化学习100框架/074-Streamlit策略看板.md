# Day74 - Streamlit 策略看板

> 阶段六：Quant 研究与回测基础

## 学习定位

本地展示策略指标和曲线。

今天的目标不是背语法，而是产出：**一个 strategy dashboard**。

## 前置知识

- 推荐前置：Day73 store
- 上一站：Day73
- 下一站：Day75

## 今日知识地图

- 本地展示策略指标和曲线
- strategy dashboard
- 输入输出边界
- 可复盘结果

## 核心讲解

今天的关键词是 **本地展示策略指标和曲线**。学习时不要只看语法表面，而要始终追问三件事：输入是什么、处理逻辑是什么、输出要给谁使用。这个习惯会贯穿数据分析、Quant、LLM 应用和 Agent tool 设计。

从工程角度看，今天的能力要服务于 **作品展示**。也就是说，代码不只是“能跑”，还要能被未来的自己复查：文件名清楚、变量名能表达含义、失败时有可读错误、结果能保存。

难度承接上，今天依赖 **Day73 store**；完成后会自然进入 **Day75**。如果今天卡住，优先回看 Day73，不要跳到更复杂的库或项目。

## 知识点结构

### 定义

本地展示策略指标和曲线，是把一个具体问题拆成可运行、可检查、可复用代码的过程。

### 为什么存在

它让学习从临时笔记变成长期资产。公开仓库里的每一天都应该能让读者看到：今天学什么、为什么学、怎么运行、如何检查结果。

### 最小案例

```python
import pandas as pd
import streamlit as st

st.title("Python100 Dashboard")
uploaded = st.file_uploader("Upload CSV")
if uploaded is not None:
    df = pd.read_csv(uploaded)
    st.metric("rows", len(df))
    st.dataframe(df.head())
```

### 常见错误

- 不要一次性引入多个新库
- 不要提交自己无法解释的代码

### 工程应用

- 数据分析脚本
- Quant 研究模块
- LLM 报告助手
- Agent tool / workflow
- GitHub 开源项目作品集

### 未来扩展

今天只做最小闭环。后续可以逐步加入测试、日志、配置、页面、API 或项目文档，但不要牺牲可解释性。

## 2 小时学习节奏

- 15 分钟：读定位、前置和知识地图。
- 25 分钟：手打最小案例并运行。
- 35 分钟：完成 7 道简单路线题。
- 35 分钟：完成 5 道基础巩固题。
- 10 分钟：记录 Debug、结果和下一步。

## 简单路线 7 题

1. 读完“学习定位”，用一句话写下今天要解决的问题。
2. 手打最小案例并运行，确认得到 `一个 strategy dashboard`。
3. 把示例输入改成 2-3 条虚拟样例，观察输出是否仍然合理。
4. 故意制造一个小错误，记录报错、原因和修复方式。
5. 给输出加一个清晰字段名或标题，避免只看到裸数字。
6. 把今天代码保存到本地练习文件夹，并写下运行命令。
7. 用 3 行话说明它如何服务于 `作品展示`。

## 5 道基础巩固题

#### 1. 复现任务

题目：完整复现今日最小案例，不改代码结构，只确认能运行。

讲解：训练“先跑通，再优化”的习惯。

参考解法骨架：

```python
# Day74 - 复现任务
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day74 复现任务")
```

#### 2. 输入替换

题目：替换为新的虚拟输入，观察输出是否仍然合理。

讲解：训练输入边界意识。

参考解法骨架：

```python
# Day74 - 输入替换
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day74 输入替换")
```

#### 3. 边界检查

题目：添加一个空输入、缺字段或非法值检查。

讲解：工业代码必须能解释失败。

参考解法骨架：

```python
# Day74 - 边界检查
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day74 边界检查")
```

#### 4. 结果保存

题目：把输出保存成 Markdown、JSON、CSV、SQLite 记录或截图说明。

讲解：公开项目需要可复查产物。

参考解法骨架：

```python
# Day74 - 结果保存
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day74 结果保存")
```

#### 5. 迁移说明

题目：写一段说明：今天能力如何迁移到 作品展示。

讲解：把语法学习变成项目资产。

参考解法骨架：

```python
# Day74 - 迁移说明
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day74 迁移说明")
```

## Hot100 / LeetCode 挑战（基础完成后）

> 先完成当天主线的 7 + 5 题，再做这一题；它是面试/工业算法线，不替代项目训练。Day21-Day35 以 Easy/Medium 为主，Day60 开始逐步进入 Medium/Hard。

- 关联题：[House Robber II](https://leetcode.com/problems/house-robber-ii/)
- 难度：Medium
- 题型：动态规划 / 环形约束
- 为什么放在今天：策略看板之后，练在原模型上加入现实约束。
- 带注释解题提示：环形拆成两种线性情况：不选第一间或不选最后一间；注释说明长度 1 的特例。
- 完成标准：写出暴力思路、优化思路、时间复杂度和 3 个边界用例；Hard 题允许拆成两天，但要保留复盘记录。

## 强化练习与工程使用

> 所属阶段：Quant 研究与回测基础。目标是：把行情样例变成可复查的研究实验。

### 工程使用场景

今天的主题是 **Streamlit 策略看板**，不要只把它当成一个孤立知识点。它应该落到 `Quant Research Starter` 里的一个可复用能力：输入清楚、处理过程可检查、输出可以保存，失败时能解释原因。重点检查：未来函数、样本区间、交易成本、风险指标。

### 强化 7 题

1. **复现题**：不看答案重写今天最小案例，并写出运行命令和预期输出。
2. **边界题**：构造 3 个异常输入，说明程序应该返回结果、跳过、还是报错。
3. **数据题**：准备 5-10 条虚拟样例数据，覆盖正常值、空值、重复值和极端值。
4. **函数题**：把核心逻辑封装成 1 个函数，函数名、参数名和返回值必须能读懂。
5. **测试题**：写 2 个正常测试和 1 个边界测试，确认修改后行为不变。
6. **算法题**：完成 [Binary Search](https://leetcode.com/problems/binary-search/)，题型：二分查找。提示：在有序数据中快速定位，训练边界条件。
7. **项目题**：把今天结果接入 `Quant Research Starter` 的一个小模块，并在 README 里写 3 行使用说明。

### 难度控制

Medium 到 Hard 过渡，重点是滑动窗口、前缀和、DP 和边界条件。 把题目连接到收益、回撤、仓位、成本、样本区间和实验记录。 如果算法题暂时写不出完整代码，先写暴力思路、输入输出样例和复杂度，再补优化版。

## Debug 记录模板

```text
错误现象：
触发输入：
定位过程：
修复方式：
以后如何避免：
```

## 复习检查

- [ ] 我能解释今天能力的输入、处理和输出。
- [ ] 我能从零手打最小案例。
- [ ] 我完成了 7 道简单路线题和 5 道基础巩固题。
- [ ] 我把结果保存成可复查产物。

## 参考资料

- [Python 官方文档](https://docs.python.org/3/)
