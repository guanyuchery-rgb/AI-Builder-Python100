# Day17 - logging 与 Debug 记录

> 阶段二：Python 工程化基本功

## 学习定位

用日志记录运行过程和错误。

今天的目标不是背语法，而是产出：**一个有开始、成功、失败日志的脚本**。

## 前置知识

- 推荐前置：Day16 CLI
- 上一站：Day16
- 下一站：Day18

## 今日知识地图

- logging
- info/error
- 异常上下文
- 可复盘

## 核心讲解

今天的关键词是 **用日志记录运行过程和错误**。学习时不要只看语法表面，而要始终追问三件事：输入是什么、处理逻辑是什么、输出要给谁使用。这个习惯会贯穿数据分析、Quant、LLM 应用和 Agent tool 设计。

从工程角度看，今天的能力要服务于 **项目调试、Agent 执行记录和线上排错**。也就是说，代码不只是“能跑”，还要能被未来的自己复查：文件名清楚、变量名能表达含义、失败时有可读错误、结果能保存。

难度承接上，今天依赖 **Day16 CLI**；完成后会自然进入 **Day18**。如果今天卡住，优先回看 Day16，不要跳到更复杂的库或项目。

## 知识点结构

### 定义

用日志记录运行过程和错误，是把一个具体问题拆成可运行、可检查、可复用代码的过程。

### 为什么存在

它让学习从临时笔记变成长期资产。公开仓库里的每一天都应该能让读者看到：今天学什么、为什么学、怎么运行、如何检查结果。

### 最小案例

```python
import logging

logging.basicConfig(level=logging.INFO, format="%(levelname)s:%(message)s")
try:
    value = float("0.12")
    logging.info("parsed value=%s", value)
except ValueError as exc:
    logging.error("parse failed: %s", exc)
```

### 常见错误

- 不要一次性引入多个新库
- 不要提交自己无法解释的代码

### 工程应用

- 数据分析脚本
- Quant 研究模块
- LLM 报告助手
- Agent tool / workflow
- GitHub 开源项目作品集

### 未来扩展

今天只做最小闭环。后续可以逐步加入测试、日志、配置、页面、API 或项目文档，但不要牺牲可解释性。

## 基础详细讲解

### logging 与 Debug 记录

`print()` 适合早期观察，`logging` 适合长期维护。日志要回答：程序走到哪里、用了什么输入、关键结果是什么、哪里失败。Debug 不是情绪记录，而是定位证据。

### 手写顺序

- 用 `logging.info()` 记录正常步骤，用 `logging.error()` 记录失败。
- 日志不要太多，重点记录输入规模、输出位置和异常原因。
- 每次修错后写一行“以后如何避免”，形成错误库。

### 今天必须真正弄懂

- **输入**：今天这段代码从哪里拿到数据，是手写值、终端输入、文件，还是函数参数。
- **处理**：每一行代码改变了什么状态，例如变量变了、列表多了一个元素、判断走了某个分支。
- **输出**：结果给谁看，是终端输出、返回值、文件，还是后面函数继续使用。

### 和后面项目的关系

Agent 可观测性、API 排错、数据任务失败定位，都依赖日志。今天开始建立工程复盘习惯。

### 零基础拆解：日志和 Debug 证据

#### 这一天到底在练什么

日志是程序运行时留下的证据。它应该说明程序走到哪一步、处理了多少数据、结果保存在哪里、失败原因是什么。

#### 手写时按这个顺序

- 先用 `logging.info()` 记录正常步骤。
- 在关键输入和输出处各写一条日志。
- 遇到异常时记录错误原因和触发数据。

#### 最常见卡点

日志太少会定位不了，太多会看不清。初学只记录输入规模、关键步骤和输出位置。

#### 自查问题

- 我能看日志复盘程序流程。
- 我能区分 info 和 error。
- 我能写一条可复查的 Debug 记录。

## 2 小时学习节奏

- 15 分钟：读定位、前置和知识地图。
- 25 分钟：手打最小案例并运行。
- 35 分钟：完成 7 道简单路线题。
- 35 分钟：完成 5 道基础巩固题。
- 10 分钟：记录 Debug、结果和下一步。

## 简单路线 7 题

1. 读完“学习定位”，用一句话写下今天要解决的问题。
2. 手打最小案例并运行，确认得到 `一个有开始、成功、失败日志的脚本`。
3. 把示例输入改成 2-3 条虚拟样例，观察输出是否仍然合理。
4. 故意制造一个小错误，记录报错、原因和修复方式。
5. 给输出加一个清晰字段名或标题，避免只看到裸数字。
6. 把今天代码保存到本地练习文件夹，并写下运行命令。
7. 用 3 行话说明它如何服务于 `项目调试、Agent 执行记录和线上排错`。

## 5 道基础巩固题

#### 1. 复现任务

题目：完整复现今日最小案例，不改代码结构，只确认能运行。

讲解：训练“先跑通，再优化”的习惯。

参考解法骨架：

```python
# Day17 - 复现任务
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day17 复现任务")
```

#### 2. 输入替换

题目：替换为新的虚拟输入，观察输出是否仍然合理。

讲解：训练输入边界意识。

参考解法骨架：

```python
# Day17 - 输入替换
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day17 输入替换")
```

#### 3. 边界检查

题目：添加一个空输入、缺字段或非法值检查。

讲解：工业代码必须能解释失败。

参考解法骨架：

```python
# Day17 - 边界检查
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day17 边界检查")
```

#### 4. 结果保存

题目：把输出保存成 Markdown、JSON、CSV、SQLite 记录或截图说明。

讲解：公开项目需要可复查产物。

参考解法骨架：

```python
# Day17 - 结果保存
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day17 结果保存")
```

#### 5. 迁移说明

题目：写一段说明：今天能力如何迁移到 项目调试、Agent 执行记录和线上排错。

讲解：把语法学习变成项目资产。

参考解法骨架：

```python
# Day17 - 迁移说明
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day17 迁移说明")
```

## Debug 记录模板

```text
错误现象：
触发输入：
定位过程：
修复方式：
以后如何避免：
```

## 复习检查

- [ ] 我能解释今天能力的输入、处理和输出。
- [ ] 我能从零手打最小案例。
- [ ] 我完成了 7 道简单路线题和 5 道基础巩固题。
- [ ] 我把结果保存成可复查产物。

## 参考资料

- [Python 官方文档](https://docs.python.org/3/)
