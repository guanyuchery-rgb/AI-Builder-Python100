# Day11 - CSV、JSON 与结构化数据

> 阶段二：Python 工程化基本功

## 学习定位

读写 CSV/JSON，理解结构化文件。

今天的目标不是背定义，而是产出：**一个能保存并读取结构化记录的脚本**。

## 前置知识

- 推荐前置：Day10 本地小工具
- 上一站：Day10
- 下一站：Day12

## 今日知识地图

- csv
- json
- DictWriter
- 结构化记录

## 真实任务入口

今天先不从定义开始，而是先看问题：**如何用「CSV、JSON 与结构化数据」完成 `一个能保存并读取结构化记录的脚本`？**

这个问题在工程里不是孤立练习。它属于 **Python 工程化基本功** 阶段，目标是：把脚本改成可复用、可调试、可测试的小工具。

先按这条顺序学习：

```text
真实任务
  ↓
拆解流程
  ↓
引出问题
  ↓
引出知识点
  ↓
解释设计原因
  ↓
代码实现
  ↓
未来项目应用
```

今天你真正要掌握的不是“会背哪个语法”，而是能回答：它解决什么问题、为什么需要它、怎么落到代码、以后在哪个项目会再次出现。

## 工程问题解决结构

### 1. 是什么（What）

CSV、JSON 与结构化数据 是用 Python 解决工程任务时的一组小工具。最简定义：它帮助你把「csv、json、DictWriter」放进可运行流程里。先把它当成解决问题的方法，不要先背术语。

### 2. 为什么存在（Why）

如果没有 CSV、JSON 与结构化数据，代码通常会变成一次性脚本：逻辑散、结果难复查、报错难定位、以后项目里也难复用。它存在的意义是把真实任务拆成更清楚的步骤，让你知道哪里输入、哪里处理、哪里输出。

### 3. 真实工程场景（Real Scenario）

- 科研场景：整理实验记录、批量生成学习日志、清洗论文标题或作者字段。
- 数据分析场景：处理一小份 CSV/JSON/文本记录，输出可检查结果。
- Quant 场景：读取几条价格或收益样例，计算简单指标或生成检查日志。
- LLM/Agent 场景：给未来 Agent tool 准备稳定的输入、输出、错误信息和本地文件。

### 4. 怎么做（How）

先看场景流程，再看代码：

```text
真实小任务
  ↓
明确输入
  ↓
拆成 1-3 个步骤
  ↓
写最小代码
  ↓
打印/保存结果
  ↓
记录 Debug
```

```python
# Day11 - CSV、JSON 与结构化数据

def solve_task(sample_input):
    """把今天的真实任务压缩成一个最小可运行函数。"""
    result = f"处理完成: {sample_input}"
    return result


def main():
    sample = "虚拟样例输入"
    output = solve_task(sample)
    print(output)


if __name__ == "__main__":
    main()
```

### 5. 工程设计思维（Design）

工程师会多写一层函数、配置、输出文件或 README，是为了让代码不只今天能跑，也能被明天的自己、GitHub 访客和未来项目继续使用。

### 6. 项目中的位置（Where）

这个知识点以后会反复出现在：

- Quant 回测框架的基础函数
- FastAPI / Streamlit 的后台逻辑
- Agent tool 的输入输出处理
- 数据处理脚本
- GitHub 开源作品
- 本地学习实验室

### 7. 掌握检查

学完今天不要只问“代码跑了吗”，要能回答四个问题：

1. 它是什么？
2. 它为什么存在？
3. 它解决什么问题？
4. 它在未来 Quant / LLM / Agent 项目中怎么使用？

### 基础优化补充：问题、场景与代码

**解决什么问题（Problem）**：CSV/JSON 解决的是“数据如何从一次性输出变成可复用资产”的问题。结果不能只停留在 `print()`，字段要有名字，文件要能保存、读取、复查，并交给下一段程序继续处理。

**真实科研场景**：保存实验编号、参数、指标、备注和失败原因。CSV 适合保存多次实验结果，JSON 适合保存一次实验的完整配置。

**Quant 场景**：保存行情样例、收益率、交易信号、策略参数和回测摘要。CSV 更适合指标表，JSON 更适合配置和运行结果摘要。

**LLM/Agent 场景**：保存 tool call 参数、模型输出、错误信息和人工审查记录。JSON 适合表达结构化输入输出，CSV 适合做评估集和批量对比。

```python
import csv
from pathlib import Path

LOG_FILE = Path("learning_log.csv")


def save_records(records):
    with LOG_FILE.open("w", newline="", encoding="utf-8") as f:
        writer = csv.DictWriter(f, fieldnames=["day", "topic", "minutes"])
        writer.writeheader()
        writer.writerows(records)


def load_records():
    with LOG_FILE.open("r", newline="", encoding="utf-8") as f:
        return list(csv.DictReader(f))
```

## 基础详细讲解

### 如果你完全看不懂，先只抓这一句话

今天学的是：**CSV 像表格，JSON 像字典和列表的组合。读取后先检查结构，再分析。**

```text
CSV -> 一行一条记录，一列一个字段
JSON -> dict / list 嵌套结构
```

### CSV 怎么想

CSV：

```text
topic,minutes
python,120
sql,90
```

可以想成：

```python
[
    {"topic": "python", "minutes": "120"},
    {"topic": "sql", "minutes": "90"},
]
```

注意：从文件读出来的 `"120"` 很可能先是字符串，要计算就要转成数字。

### JSON 怎么想

```json
{"topic": "python", "minutes": 120}
```

读进 Python 后很像：

```python
{"topic": "python", "minutes": 120}
```

所以 JSON 和 dict 的关系非常近。

### 读取后先做三件事

| 检查 | 为什么 |
| --- | --- |
| 打印类型 | 确认是 list 还是 dict |
| 打印数量 | 确认读到了多少条 |
| 打印第一条 | 确认字段名和数据形状 |

### Debug 对照表

| 现象 | 常见原因 | 怎么改 |
| --- | --- | --- |
| 字段取不到 | 字段名拼错或表头不同 | 打印所有 key |
| 数字不能相加 | 读出来是字符串 | 用 `int()` 或 `float()` 转换 |
| JSON 报错 | 格式不是合法 JSON | 检查引号、逗号、括号 |
| 循环层级错 | 把 dict 当 list 或把 list 当 dict | 先打印 `type(data)` |

### CSV、JSON 与结构化数据

CSV 像表格，JSON 像嵌套字典和列表。学习它们的重点不是记格式，而是知道数据从哪里来、字段是什么意思、读取后变成什么 Python 对象。

### 基础知识深讲：结构化数据和字段意识

CSV 和 JSON 的共同点是把数据结构显式保存下来。CSV 更像二维表，每一行是一条记录，每一列是一个字段；JSON 更像 Python 的 dict/list，可以表达嵌套结构。理解格式差异，才能决定后面用什么方式读取和验证。

读取结构化数据后，不要马上计算。第一步应该做数据体检：顶层类型是什么、总共有多少条、字段名有哪些、第一条样例长什么样。这个流程能提前发现字段拼写、缺失值、类型不一致和格式误读。

字段意识是数据分析的基础。`price`、`date`、`volume`、`topic` 都不是普通变量名，它们代表业务含义。代码里每次取字段，都应该能说清这个字段从哪里来、单位是什么、是否允许为空。

JSON 尤其要注意层级。`data["items"][0]["title"]` 这种路径表示从字典进入列表，再进入字典。以后调用 LLM API、读取配置、处理 Agent tool 输出时，嵌套结构会非常常见。

### 手写顺序

- CSV 先看表头，再逐行读取；字段名要和代码里的 key 对得上。
- JSON 先打印顶层类型：是 dict 还是 list，再逐层取字段。
- 读完数据后先做数量、字段、样例三项检查。

### 今天必须真正弄懂

- **输入**：今天这段代码从哪里拿到数据，是手写值、终端输入、文件，还是函数参数。
- **处理**：每一行代码改变了什么状态，例如变量变了、列表多了一个元素、判断走了某个分支。
- **输出**：结果给谁看，是终端输出、返回值、文件，还是后面函数继续使用。

### 和后面项目的关系

API 响应、配置文件、评估集和交易样例都会用 CSV/JSON。今天是在练数据入口。

### 零基础拆解：CSV/JSON 数据入口

#### 这一天到底在练什么

CSV 像表格，JSON 像 Python 里的 dict/list 组合。今天重点是读进来之后先检查结构，而不是马上分析。

#### 手写时按这个顺序

- 先准备 3 行 CSV 或一个小 JSON。
- 读取后打印数据数量和第一条样例。
- 确认字段名和代码里使用的 key 一致。

#### 最常见卡点

字段名写错、编码不一致、JSON 顶层类型看错，是最常见问题。先打印类型和样例再继续。

#### 自查问题

- 我能说出 CSV 和 JSON 分别适合什么。
- 我能打印第一条样例检查字段。
- 我能处理缺少字段的记录。

## 工程工作流补充

### 如果你完全看不懂，先只抓这一句话

今天这章不是孤立知识点，而是在训练一个真实工程习惯：把「CSV、JSON与结构化数据」放进一个可以运行、可以检查、可以复用的小流程里。

这一阶段的目标是把基础语法变成可复用的小能力。你要从“我会写一段代码”，过渡到“我知道这段代码放在哪里、输入是什么、输出是什么、失败时怎么查”。

### 真实工作流怎么想

你可以按下面这条线理解今天内容：

```text
输入数据 -> 选择数据结构 -> 写成函数 -> 打印/保存结果 -> 记录错误
```

这条线的意思是：先不要追求写得高级。先保证你知道数据从哪里来，经过了哪些变量，最后去了哪里。只要能画出这条数据流，你就已经从“背写法”进入“解决问题”。

### 执行过程追踪

学习今天内容时，每写 5 到 10 行代码，就问自己四个问题：

1. 现在有哪些变量？
2. 每个变量是什么类型？
3. 下一行代码会改变哪个变量？
4. 如果输出不对，我应该打印哪里？

这四个问题比多背几个函数更重要。以后做数据分析、Agent tool、量化回测时，真正救命的就是这种追踪能力。

### 最容易混的点

- 不要只看最终答案，要看中间变量怎么变化。
- 不要把函数、循环、文件、列表当成分开的知识，它们最后都会组合在一个任务里。
- 不要一开始就追求“优雅写法”。先写清楚，再减少重复。
- 每个例子都要能说清楚 input、process、output。

### 今日最小工程化练习

在今天主代码旁边新建一个 `day11_mini_project.py`，只做三件事：

1. 准备一小份样例数据。
2. 用今天知识点处理它。
3. 打印一个清晰结果，并写一句注释解释这个结果代表什么。

完成后再想一步：如果明天要复用这段代码，哪些地方应该改成函数，哪些参数不应该写死。

### 知识广度补充：CSV/JSON 还要理解读写边界

CSV/JSON 不是只会 `load` 和 `dump`。真实项目里还要注意：

- CSV 每一列最好有稳定字段名。
- JSON 支持嵌套结构，但太深会难维护。
- 读取后先检查字段是否存在。
- 数字读出来可能是字符串，要做类型转换。
- 保存文件时考虑是否覆盖旧文件。

```python
record = {"ticker": "AAPL", "price": "100.5"}

if "price" in record:
    price = float(record["price"])
    print(price)
```

### 工业化知识百科补充：从会用到会迁移

这一节不是把前面没学完的语法硬塞回来，而是把 `CSV、JSON 与结构化数据` 放进后续真实项目里补厚。你现在已经进入 Day10 之后，学习重点要从“我知道这个语法”升级为“我知道它在工程里解决什么问题、会踩什么坑、怎么迁移到项目”。

#### 1. 阶段定位

当前阶段：**工程化基本功**。

阶段目标：把零散语法变成能反复运行、能检查、能复盘的小工具。

本阶段反复出现的能力：

- 输入校验
- 循环控制
- 函数边界
- 文件持久化
- 日志与测试
- 命令行参数

#### 2. 这个主题必须补齐的知识面

- 表格字段
- 嵌套结构
- 编码
- 类型转换
- 缺字段处理
- 读写模式

学习时不要把这些点当成孤立词条。更好的读法是：每个概念都对应一种工程压力，例如输入变多、数据变脏、文件变大、结果要复现、输出要交给别人、模型回答需要校验。

#### 3. 真实项目里怎么用

- **数据分析**：先明确字段、类型、缺失值、样本范围，再写处理逻辑。不要直接对未知数据写复杂代码。
- **Quant**：任何计算都要关心时间顺序、样本区间、交易成本、未来函数和结果复现。
- **LLM/Agent**：所有输入输出都要有结构，模型结果要校验，高风险动作要有人类审查点。
- **开源作品集**：代码之外还要有 README、样例输入、运行命令、测试和复盘说明。

#### 4. 常见坑和边界

- CSV 读出来通常都是字符串，要显式转换数字。
- JSON 结构不要嵌套过深，否则后续校验困难。
- 字段名要稳定，避免今天叫 price 明天叫 close。
- 不要把所有逻辑堆进一个文件或一个函数。先拆出最小核心函数，再决定是否需要 CLI、API、看板或测试。
- 不要用“能跑一次”代表掌握。真正掌握是换一份输入、换一个场景、换一个项目后仍然能迁移。

#### 5. 最小迁移代码骨架

```python
def day011_engineering_note(input_data):
    """记录今天知识点在工程项目里的使用边界。"""
    return {
        "day": "Day11",
        "topic": "CSV、JSON 与结构化数据",
        "input_shape": "明确输入来源、字段、类型和边界",
        "core_step": "把今天主题压缩成一个可测试的小函数",
        "output_shape": "明确输出给终端、文件、API、报告还是下游函数",
        "failure_modes": ["空输入", "类型不对", "缺字段", "路径或网络失败"],
        "reuse_target": "数据分析 / Quant / LLM-Agent / 作品集项目",
    }
```

#### 6. 百科式自查问题

1. 这个主题的输入是什么？输入可能为空、缺字段、类型错误吗？
2. 核心处理步骤能不能写成一个小函数？
3. 输出是给人看，还是给下游程序继续用？
4. 失败时怎么记录？是 `print`、`logging`、错误报告，还是测试失败？
5. 如果迁移到 Quant / LLM / Agent 项目，最大的风险是什么？
6. 这个能力是否需要 README、样例数据或测试来长期保存？

#### 7. 建议扩展练习

- 写一个最小正常样例。
- 写一个边界样例，例如空输入、缺字段、类型错误、路径不存在。
- 写一段 Debug 记录，说明错误现象、定位过程和修复方式。
- 把今天的代码改造成一个函数，并说明输入和输出。
- 思考它未来会放进哪个项目模块：`data_io`、`transform`、`analysis`、`api`、`app`、`agent_tool`、`backtest` 或 `report`。

## 多元化操作题（从 Day11 开始）

1. 把 5 条学习记录保存为 CSV。
2. 读取 JSON 配置并校验字段。
3. 写一个带 --input/--output 的 CLI。
4. 跳过空记录并继续处理。
5. 遇到第一个严重错误就 break。
6. 把结果追加写入日志文件。
7. 把核心逻辑拆成函数并测试。
8. 把输出改成 Agent tool 的 ok/data/error 格式。

## Hot100 / LeetCode 题组（多题 + 多解法）

1. [Two Sum - 两数之和](https://leetcode.com/problems/two-sum/)
   - 题型：数组 / 哈希表
   - 多解法：暴力双循环；哈希表一次遍历；排序+双指针但要保留下标
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：先把问题翻译成“当前数还差谁”。暴力法是在所有组合里找答案；哈希表法一边走一边记住见过的数，把查找从 O(n) 降到接近 O(1)。重点是先查 need，再存 num，避免同一个元素用两次。
   - 解题步骤：先明确要快速查询什么，再用 dict/set/Counter 保存已经见过的状态。
   - 易错点：最容易错的是先查再存还是先存再查，以及重复值、空输入和 key 的设计。
2. [Group Anagrams - 字母异位词分组](https://leetcode.com/problems/group-anagrams/)
   - 题型：哈希表 / 字符串分组
   - 多解法：排序签名分组；字符计数元组；Counter 转 tuple
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：把每个字符串映射成稳定签名，再按签名分组。训练的是“为对象设计可比较 key”的能力，可迁移到日志聚类、标签归并和文本特征分桶。
   - 解题步骤：先明确要快速查询什么，再用 dict/set/Counter 保存已经见过的状态。
   - 易错点：最容易错的是先查再存还是先存再查，以及重复值、空输入和 key 的设计。
3. [Product of Array Except Self - 除自身以外数组的乘积](https://leetcode.com/problems/product-of-array-except-self/)
   - 题型：数组 / 前后缀
   - 多解法：左右乘积数组；两次扫描常数空间；处理零的特判
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：不能用除法时，用左侧乘积和右侧乘积组合答案。关键是分清当前位置不参与自己的结果，工程里常迁移到前后文特征构造。
   - 解题步骤：先用暴力解法确认输入输出，再找可以复用的状态变量进行优化。
   - 易错点：最容易错的是没有先定义清楚状态变量、边界和返回值。
4. [Maximum Subarray - 最大子数组和](https://leetcode.com/problems/maximum-subarray/)
   - 题型：动态规划 / 贪心
   - 多解法：暴力区间；Kadane；前缀和最小值
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：每一步决定“接着前面的区间”还是“从当前重新开始”。它训练局部最优状态更新，量化里可迁移到连续收益段和回撤分析。
   - 解题步骤：先定义 dp 状态，再写 base case，最后写状态转移并用小样例手算一遍。
   - 易错点：最容易错的是 base case、数组长度、不可达状态和状态转移方向。
5. [Merge Intervals - 合并区间](https://leetcode.com/problems/merge-intervals/)
   - 题型：排序 / 区间
   - 多解法：排序后扫描；栈保存结果；原地维护末尾区间
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：先按起点排序，再判断当前区间是否和结果末尾重叠。核心是把无序冲突整理成有序扫描，适合迁移到时间段合并、事件窗口处理。
   - 解题步骤：先按起点或终点排序，再维护当前区间或结果末尾区间。
   - 易错点：最容易错的是重叠判断、边界等号和插入前后顺序。
6. [Insert Interval - 插入区间](https://leetcode.com/problems/insert-interval/)
   - 题型：区间 / 模拟
   - 多解法：三段扫描；合并后排序；一次遍历状态机
   - 迁移：写清输入、状态变量、输出和失败边界。
   - 解析：把区间分成插入前、重叠合并、插入后三类。训练的是边界分类能力，适合会议时间、事件窗口、数据有效期处理。
   - 解题步骤：先按起点或终点排序，再维护当前区间或结果末尾区间。
   - 易错点：最容易错的是重叠判断、边界等号和插入前后顺序。

### 一题多解法拆解：Two Sum

- 解法 1：暴力双循环。
- 解法 2：哈希表一次遍历。
- 解法 3：排序+双指针但要保留下标。

推荐先写最容易懂的版本。

再写复杂度更好的版本。

最后写一句：这个解法能迁移到今天哪个操作题。

### 带注释参考代码：Two Sum

```python
def two_sum(nums, target):
    seen = {}  # 值 -> 下标
    for i, num in enumerate(nums):
        need = target - num
        if need in seen:
            return [seen[need], i]
        seen[num] = i
    return []
```

### 做题复盘模板

```text
题目：
LeetCode 官方链接：
提交日期：
提交结果：未做 / 已尝试 / Accepted
输入：
输出：
暴力解法：
优化解法：
关键状态变量：
最容易错的边界：
一题多解法对比：
能迁移到今天哪个操作题：
可写进简历/面试的表达：
```

## 2 小时学习节奏

- 15 分钟：读定位、前置和知识地图。
- 25 分钟：手打最小案例并运行。
- 35 分钟：完成 7 道简单路线题。
- 35 分钟：完成 5 道基础巩固题。
- 10 分钟：记录 Debug、结果和下一步。

## 简单路线 7 题

1. 读完“学习定位”，用一句话写下今天要解决的问题。
2. 手打最小案例并运行，确认得到 `一个能保存并读取结构化记录的脚本`。
3. 把示例输入改成 2-3 条虚拟样例，观察输出是否仍然合理。
4. 故意制造一个小错误，记录报错、原因和修复方式。
5. 给输出加一个清晰字段名或标题，避免只看到裸数字。
6. 把今天代码保存到本地练习文件夹，并写下运行命令。
7. 用 3 行话说明它如何服务于 `数据分析和 Agent tool 的输入输出`。

## 5 道基础巩固题

#### 1. 复现任务

题目：完整复现今日最小案例，不改代码结构，只确认能运行。

讲解：训练“先跑通，再优化”的习惯。

参考解法骨架：

```python
# Day11 - 复现任务
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day11 复现任务")
```

#### 2. 输入替换

题目：替换为新的虚拟输入，观察输出是否仍然合理。

讲解：训练输入边界意识。

参考解法骨架：

```python
# Day11 - 输入替换
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day11 输入替换")
```

#### 3. 边界检查

题目：添加一个空输入、缺字段或非法值检查。

讲解：工业代码必须能解释失败。

参考解法骨架：

```python
# Day11 - 边界检查
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day11 边界检查")
```

#### 4. 结果保存

题目：把输出保存成 Markdown、JSON、CSV、SQLite 记录或截图说明。

讲解：公开项目需要可复查产物。

参考解法骨架：

```python
# Day11 - 结果保存
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day11 结果保存")
```

#### 5. 迁移说明

题目：写一段说明：今天能力如何迁移到 数据分析和 Agent tool 的输入输出。

讲解：把语法学习变成项目资产。

参考解法骨架：

```python
# Day11 - 迁移说明
# 1. 准备输入
# 2. 调用今天的核心能力
# 3. 检查输出并记录 Debug
print("run Day11 迁移说明")
```

## Debug 记录模板

```text
错误现象：
触发输入：
定位过程：
修复方式：
以后如何避免：
```

## 复习检查

- [ ] 我能解释今天能力的输入、处理和输出。
- [ ] 我能从零手打最小案例。
- [ ] 我完成了 7 道简单路线题和 5 道基础巩固题。
- [ ] 我把结果保存成可复查产物。

## 参考资料

- [Python 官方文档](https://docs.python.org/3/)
