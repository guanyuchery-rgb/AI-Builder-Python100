# Day43 - Embedding 与向量相似度

学习定位：不接外部模型，先理解向量相似度。今天只手写 cosine similarity。

## 今日前置

- 已完成前面天数的基础能力。
- 今天只增加一个小能力，不把后面项目一次性做完。
- 如果卡住，先回到最小案例，不直接堆新库。

## 今日只允许新增

- list[float]
- dot product
- norm
- cosine

## 今日不要做

- 不要今天接向量数据库；不要讨论大规模检索优化。
- 不使用真实身份、真实账号、真实密钥或不可公开数据。
- 不把不能解释的代码提交到项目里。

## 2 小时学习节奏

- 15 分钟：读学习定位和知识地图。
- 25 分钟：手打最小案例并运行。
- 35 分钟：完成 7 道简单路线题。
- 35 分钟：完成 5 道基础巩固题。
- 10 分钟：记录 Debug 和下一步。

## 今日知识地图

- Embedding：检索、推荐、RAG 都依赖相似度。
- Cosine Similarity：它是最常见的向量检索基础。

## 知识点 1：Embedding

### 定义

Embedding 是文本或对象的向量表示。

### 为什么存在

检索、推荐、RAG 都依赖相似度。

### 最小案例

看下面“今日最小可运行任务”，先跑通，再改一处输入。

### 常见错误

- 没有先确认输入字段和输出格式。
- 直接复制复杂代码，但解释不清每一步。
- 跑通后没有保存结果或记录失败情况。

### 工程应用

- 数据分析脚本。
- Quant 研究小模块。
- LLM / Agent 工具边界。
- 个人作品集项目。

### 未来扩展

今天只做最小版本；未来可以加测试、日志、配置、服务接口或页面。

## 知识点 2：Cosine Similarity

### 定义

余弦相似度衡量两个向量方向接近程度。

### 为什么存在

它是最常见的向量检索基础。

### 最小案例

看下面“今日最小可运行任务”，先跑通，再改一处输入。

### 常见错误

- 没有先确认输入字段和输出格式。
- 直接复制复杂代码，但解释不清每一步。
- 跑通后没有保存结果或记录失败情况。

### 工程应用

- 数据分析脚本。
- Quant 研究小模块。
- LLM / Agent 工具边界。
- 个人作品集项目。

### 未来扩展

今天只做最小版本；未来可以加测试、日志、配置、服务接口或页面。

## 今日最小可运行任务

```python
import math

def cosine(a: list[float], b: list[float]) -> float:
    dot = sum(x * y for x, y in zip(a, b))
    norm_a = math.sqrt(sum(x * x for x in a))
    norm_b = math.sqrt(sum(y * y for y in b))
    return dot / (norm_a * norm_b)

print(cosine([1, 0, 1], [1, 1, 0]))
```

## Debug 日志

记录这三件事：

- 输入是什么。
- 程序在哪一步失败。
- 修复后输出是什么。

## Quant / LLM / Agent 迁移点

今天的交付物是：能解释并计算两个向量的相似度。它以后可以作为 Quant 数据模块、LLM 报告模块或 Agent tool 的一个稳定零件。

## 简单路线 7 题（不超前）

1. 新建 `day43` 文件夹或脚本。
2. 手打一遍“今日最小可运行任务”，不要复制后直接跳过。
3. 把示例数据改成 2-3 条自己的虚拟数据。
4. 故意制造一个小错误，记录报错信息和修复方式。
5. 给输出结果加一个清晰字段名或标题。
6. 把运行命令写进当天笔记。
7. 用 3 行话总结今天能力如何迁移到 能解释并计算两个向量的相似度。

## 5 道基础巩固题

#### 1. 复现最小案例

题目：完整手打最小案例，并确认输出和预期一致。

讲解：先不改结构，只确认能跑。

参考解法骨架：

```python
# Day43 - 复现最小案例
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day43 复现最小案例")
```

#### 2. 替换输入数据

题目：把示例里的数据改成自己的虚拟数据。

讲解：只改输入，不改处理逻辑。

参考解法骨架：

```python
# Day43 - 替换输入数据
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day43 替换输入数据")
```

#### 3. 增加边界检查

题目：为空数据、缺字段或异常输入加一个检查。

讲解：检查失败时返回清晰错误。

参考解法骨架：

```python
# Day43 - 增加边界检查
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day43 增加边界检查")
```

#### 4. 保存结果

题目：把结果保存为 `.md`、`.json`、`.csv` 或数据库记录。

讲解：工业项目不能只依赖 print。

参考解法骨架：

```python
# Day43 - 保存结果
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day43 保存结果")
```

#### 5. 写复盘说明

题目：写下今天的输入、处理、输出、失败情况。

讲解：保证六个月后还能重新接上。

参考解法骨架：

```python
# Day43 - 写复盘说明
# 1. 准备输入
# 2. 调用今天的最小能力
# 3. 检查输出并记录 Debug
print("TODO: run Day43 写复盘说明")
```

## 复习检查

- [ ] 我能解释今天新增的关键词。
- [ ] 我能从零手打最小案例。
- [ ] 我知道今天输入、处理、输出分别是什么。
- [ ] 我完成了 7 道简单路线题和 5 道基础巩固题。
- [ ] 我没有暴露真实身份、账号、密钥或私人数据。

## 参考资料

- [Python 官方文档](https://docs.python.org/3/)
